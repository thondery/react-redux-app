'use strict'

import * as types from '../constants/actionTypes'

export function getTopicList (listinfo) {
  return dispatch => {
    dispatch({
      type: types.GETINDEXLIST,
      data: ['lsflslflf']
    })
  }
}